import React from 'react'
import { View } from 'react-native';

function Toolbar(props) {
    return (
        <View>
            <Text>
                "Toolbar"
            </Text>
        </View>
    );
}

export default Toolbar;